/renewal-plan
you are writing a Front-Backend separated project contract apis based on PHP code.
the FE will imitate the old ui, and hardcode some fixed text, but most data need BE to provide.
In general, I think in most time 2 apis are enough, one for status and info /status, one for register contract /register.
old system will call sendAccessToken, you ignore this thing in new system.
be more careful about these:
- php code in soft link link-apotool_master -> ../apotool_master
- if need to update contracts table, should use adminservice api, code in link-admin-backend -> ../admin-backend backend api of src/usecase/contractusecase/update_contract_for_external_usecase.go
- output date format use repo’s as standard, php format not need exactly copy
/register special rules
- obey the old php post format, if the old uses form for upload bin or something, the new will use form. Othervise use json.
- be precise of every field, no useless fields, and naming obey old tradition
- correlated 3rd service.
- only comment for biz code if necessary, never comment DDD code that we all know
- Where conditions best split by lines
- post and write api is never allowed to use query, only use repository
new: POST /contract/v1/contracts/line/activate
old: /user/line_contract/activate
