  ---
  Bug: Lock Order Comment Is Misleading

  The updated comment in change_visiting_status.go:33 states:

  // Lock order: (1) reservation -> (2) office -> (3) oncall.

  But the actual runtime order is:

  ┌──────┬────────────────────────────────────────────┬──────────────────────────────────┐
  │ Step │                    What                    │              Where               │
  ├──────┼────────────────────────────────────────────┼──────────────────────────────────┤
  │ (1)  │ Lock reservation (FOR UPDATE)              │ changeVisitingStatusCore line 35 │
  ├──────┼────────────────────────────────────────────┼──────────────────────────────────┤
  │ (3)  │ Lock oncall (FOR UPDATE via withLock=true) │ processOncallArrived line 113    │
  ├──────┼────────────────────────────────────────────┼──────────────────────────────────┤
  │ (2)  │ Lock office (FOR UPDATE)                   │ createOncallReservation line 220 │
  └──────┴────────────────────────────────────────────┴──────────────────────────────────┘

  The oncall row is locked before the office row. The actual acquisition order is (1) reservation → (3) oncall → (2) office.

  Risk: This doesn't cause a deadlock today because the office lock only happens inside createOncallReservation (the "not found" branch at line 134). But the comment misleads future developers — if someone adds a new path that
  locks office before oncall based on this comment, they'd introduce a real deadlock.

  Suggestion: Renumber the comments to match reality, or clarify that locking is conditional:

  // Lock order: (1) reservation -> (2) oncall -> (3) office (conditional, only on create).