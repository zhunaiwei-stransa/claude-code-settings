  1. BUG -- Mock has real business logic instead of m.Called()

  apps/apotool/internal/misc/mockpkg/mockquery/mock_user_query.go:50-90

  MockUserQuery.ResolveTermConsentDismissible contains a full reimplementation of the grace-period calculation (date parsing, deadline math, calling m.FetchFirstLoginSince), while every other method in this mock properly
  delegates to m.Called().

  Problems:
  - Tests cannot stub the return value. Any test that needs to control whether dismissible is true or false must set up FetchFirstLoginSince expectations and craft dates to hit the right branch, rather than simply doing
  m.On("ResolveTermConsentDismissible", ...).Return(false, nil).
  - Duplicated logic in 2 places (user_query_impl.go and the mock). If the grace calculation changes, the mock must be updated in sync or tests silently diverge from prod.
  - Duplicated termConsentGraceDays constant (mock file line 14 vs. user_query_impl.go line 25).
  - Unexpected FetchFirstLoginSince calls -- a test that only expects ResolveTermConsentDismissible will panic on an unexpected FetchFirstLoginSince call made internally by the mock.

  Fix: Replace the body with the standard m.Called(...) pattern, matching the other methods.