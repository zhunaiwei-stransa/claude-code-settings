/renewal-plan
you are writing a Front-Backend separated project contract apis based on PHP code.
In general, I think 2 apis are enough, one for status and info /status, one for register contract /register.
old system will call sendAccessToken, you ignore this thing in new system.
be more careful about these:
- php code in soft link link-apotool_master -> ../apotool_master
- output date format use repo’s as standard, php format not need exactly copy
- auth token is checked in other api that you dont need care about, just focus on what api should do
- only write code for /status
- /status must be a superset of old api, because we dont know how old UI use these data
- status often tells the status of current contract in english
- statusLabel often tells the status in Japanese(if old php defines label field)
- allowedAction often tells the user allowed action for now (php seems to merge status and action into one field named allow, which is not good)
- fullnaming for handler and usercase like: regiSbpsConfigQuery, fetchRegiSbpsContractStatusHandler
/status api has a unified process in all club-apotool apis, but the biz logic correctness must align with php
1. fetch all data into one object(no matter required or optional, if optional ignore notfound err)
2. this object provide method for get status or info(this object can only be defined in usecase
)
3. usecase use this object to generate output response
new: GET /contract/v1/contracts/line/status
old: /user/line_contract
